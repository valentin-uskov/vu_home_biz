export const projectsLoadingSelector = (state) => state.projects.loading;
export const projectsLoadedSelector = (state) => state.projects.loaded;
export const projectsListSelector = (state) => state.projects.entities;
// projectListSelector